{
  "data": {
    "alerts": [
      {
        "labels": {
          "alertname": "ReceiverDepositsPending",
          "client_id": "trigger",
          "job": "alert-trigger",
          "network": "Preview",
          "severity": "info"
        },
        "annotations": {
          "description": "The per-client deposit address for client trigger holds 6.00 ADA of deposits that have not yet been folded into the Receiver balance (alert fires above 5 ADA). Nothing is broken — this is the client's prepaid funding waiting to be applied.\nClient deposit address (on Preview, view on a testnet explorer):\n  \n\nWHY: clients add prepaid funds by sending ADA to their per-client deposit address (side-deposit funding) instead of running `receiver:top-up` themselves. Those deposits accumulate there until a `deposit:merge` spends them and the current Receiver UTxO together, re-creating the Receiver with the deposits folded into its spendable balance. Until a merge runs, the deposited ADA cannot pay update fees — only the Receiver's spendable balance can (see ReceiverBalanceLow). Merging is safe and idempotent; it never touches the accrued bucket.\nWHAT TO DO — run a merge for this client (folds deposits -> Receiver balance):\n  Docker: cd offchain && make cli CMD=\"deposit:merge --protocol-state /app/offchain/state//config-bootstrap.json --client-state /app/offchain/state//clients/trigger.json\"\n  npm:    cd offchain/cli && npm run cli -- deposit:merge --protocol-state ../state//config-bootstrap.json --client-state ../state//clients/trigger.json\nAfter it confirms, the deposit-pending total drops to 0 and the alert clears within ~1 minute.\n",
          "summary": "Client trigger has over 5 ADA of un-merged deposits waiting"
        },
        "state": "firing",
        "activeAt": "2026-06-18T07:02:50.29264446Z",
        "value": "6e+00"
      }
    ]
  }
}
