# Mainnet routers go here, one file per client (e.g. client-a.yaml).
# The loader reads ONLY this folder when CARDANO_NETWORK=Mainnet.
# Mirror routers/preview/client-a.yaml but with network: Mainnet,
# state/mainnet/... paths and private_key_env: CARDANO_WALLET_SEED_MAINNET.
